# finalproject_Matesheva_DPO

# ValutaTrade Hub

Консольное приложение для управления виртуальным валютным портфелем. Поддерживает фиатные и криптовалюты, покупку/продажу, просмотр курсов.

## Установка

1. Убедитесь, что установлен Python 3.10+ и Poetry.
2. Клонируйте репозиторий.
3. Выполните:
  
   make install
   
   или
  
   poetry install
   
4. Создайте файл `.env` на основе `.env.example` и укажите ваш API-ключ ExchangeRate-API (если есть).

## Запуск


make project

или

poetry run project


## Команды

| Команда                                                       | Описание                                                                                          |
| :------------------------------------------------------------- | :------------------------------------------------------------------------------------------------ |
| `register --username USERNAME --password PASSWORD`            | Зарегистрировать пользователя. Пароль > 4 символов.                                               |
| `login --username USERNAME --password PASSWORD`                | Войти в систему.                                                                                  |
| `show-portfolio [--base BASE]`                                 | Показать все кошельки в базовой валюте.                                                           |
| `buy --currency CUR --amount AMOUNT [--base BASE]`             | Купить валюту.                                                                                    |
| `sell --currency CUR --amount AMOUNT [--base BASE]`            | Продать валюту.                                                                                   |
| `get-rate --from FROM --to TO`                                 | Получить текущий курс.                                                                            |
| `show-rates [--currency CUR] [--top N] [--base BASE]`          | Показать курсы. Фильтр по валюте, топ N самых дорогих, смена базовой валюты.                      |
| `update-rates [--source coingecko\|exchangerate]`              | Однократно обновить курсы валют.                                                                  |
| `scheduler-start`                                              | Запустить фоновое обновление курсов валют.                                                        |
| `scheduler-stop`                                               | Остановить фоновое обновление.                                                                    |
| `scheduler-status`                                             | Показать, запущен ли планировщик.                                                                 |
| `exit`                                                         | Выйти из программы.                                                                               |
| `help`                                                         | Показать эту справку.                                                                             |

## Настройка

Для работы с фиатными курсами требуется ключ ExchangeRate-API. Получите его на [exchangerate-api.com](https://app.exchangerate-api.com/sign-up) и сохраните в файл `.env`:


EXCHANGERATE_API_KEY=ваш_ключ


Криптовалютные курсы получаются из CoinGecko (без ключа).

## Структура данных

- `data/users.json` — пользователи.
- `data/portfolios.json` — портфели (кошельки).
- `data/rates.json` — кэш курсов (используется для быстрого доступа).
- `data/exchange_rates.json` — история всех полученных курсов.

## TTL (время жизни кэша)

По умолчанию курсы считаются свежими в течение 5 минут (300 секунд). Настраивается в `config.json` или через `SettingsLoader`.


### Примеры использования команд

```bash
# Регистрация и вход
register --username alice --password 1234
login --username alice --password 1234

# Операции с портфелем
buy --currency BTC --amount 0.05
sell --currency BTC --amount 0.05
sell --currency BCC --amount 0.05
show-portfolio

# Получение информации о курсах
get-rate --from BTC --to USD
show-rates --top 5

# Управление обновлением курсов
scheduler-start
update-rates --source coingecko

# Выход из программы
exit


  ## 🎥 Демонстрация asciinema

asciinema play DEMO-ascii.cast
